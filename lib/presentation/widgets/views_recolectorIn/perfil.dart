import 'package:flutter/material.dart';
class perfilRecolector extends StatelessWidget {
  const perfilRecolector({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}